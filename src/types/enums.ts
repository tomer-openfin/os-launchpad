export enum AppIconSizes {
  Small = 14,
  Medium = 34,
  Large = 54,
}

export enum DirectionalPosition {
  Top = 'top',
  Right = 'right',
  Bottom = 'bottom',
  Left = 'left',
}

export enum ResponseStatus {
  SUCCESS = 'ok',
  FAILURE = 'error',
}
